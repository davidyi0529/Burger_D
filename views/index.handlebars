<h1>Burgers!</h1>

<h2>Get in my Tummy!</h2>

<!-- Pass in data from server.js -->
{{!-- <ul>
  {{#each Burgers}}
      {{> cats/cat-block sleep=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Cats that are sleepy!</h2>

<ul>
  {{#each cats}}
    {{#if sleepy}}
      {{> cats/cat-block sleep=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Cat</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Cat Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="slee">Sleepy?</label><br>
    <input type="radio" name="sleepy" value="1" checked> Sleepy!<br>
    <input type="radio" name="sleepy" value="0"> Awake
  </div>

  <button type="submit">Add Cat</button>
</form>

<h2>Add a Quote</h2>

<form class="create-form">

  <div class="form-group">
    <label for="auth">Author:</label>
    <input type="text" id="auth" name="author">
  </div>

  <div class="form-group">
    <label for="quo">Quote:</label>
    <textarea id="quo" name="quote" rows="8" cols="40"></textarea>
  </div>

  <button type="submit">Add Quote</button>
</form> --}}

{{!-- <div class="row"> 

    <div class="col-md-6">
      <div class="card">
        <h4 class="card-header text-center">Ready to eat!</h4>
        <div class="card-body">
          {{#each burgers}}
            {{#unless this.devoured}}
            
              <div class="input-group my-4">
                <li class="form-control">{{id}}. {{burger_name}}</li> <button class="btn btn-success devour-button ml-3" data-id="{{id}}" type="submit">Devour It!</button>  
              </div>
            
            {{/unless}}
          {{/each}}

        </div>
      </div>
    </div>

    <div class="col-md-6">
      <div class="card">
        <h4 class="card-header text-center">Already Eaten!</h4>
        <div class="card-body">
          {{#each burgers}}
            {{#if this.devoured}}
              <div class="input-group my-4">
                <li class="form-control">{{id}}. {{burger_name}}</li><button class="btn btn-success restore-button ml-3" data-id="{{id}}" type="submit">Ready for Seconds</button>
              </div>
            {{/if}}
          {{/each}}

        </div>
      </div>
    </div> 
</div> --}}
